{
  "name": "kinklist-prototype",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Prototype de fiabilité, performance et qualité pour KinkList.",
  "scripts": {
    "check:syntax": "for file in js/*.js; do node --check \"$file\"; done",
    "check:data": "node scripts/validate-data.mjs",
    "test": "node --test tests/*.test.mjs",
    "verify": "npm run check:syntax && npm run check:data && npm test"
  },
  "engines": {
    "node": ">=20"
  }
}
